/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com'], // Add other domains if necessary
      },
  };
  
  export default nextConfig;
  